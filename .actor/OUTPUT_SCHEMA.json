{
  "actorOutputSchemaVersion": 1,
  "title": "Made-in-China Product & Supplier Finder output",
  "description": "MCP- and API-friendly links to one-row-per-supplier results, optional CSV exports, the run summary, and diagnostic files.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "Supplier results",
      "description": "Unique Made-in-China supplier records with source inputs, matched products, pricing, MOQ, audit badges, and available public profile details.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "files": {
      "type": "string",
      "title": "CSV and diagnostic files",
      "description": "Optional CSV exports, the run summary, and any diagnostic HTML saved by the Actor.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys"
    }
  }
}
